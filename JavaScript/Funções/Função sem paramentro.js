function retorna_versao(){//Oque está entre parenteses() seria o parametro
    return 1.1;//retorna o resultado da funcao
}

let versao = retorna_versao();//variavel que está recebendo a função

console.log(versao);